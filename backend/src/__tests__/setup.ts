// Configuración global para tests unitarios
// No requiere conexión a base de datos real

// Configurar variables de entorno para testing si es necesario
process.env.NODE_ENV = 'test';

// Limpiar cualquier mock global antes de cada test
// Jest ya maneja esto con clearMocks: true, pero podemos agregar lógica adicional aquí si es necesario

